#pragma once

#ifndef MAX
#define MAX(a, b) (a < b ? b : a)
#endif
#ifndef MIN
#define MIN(a, b) (a < b ? a : b)
#endif

#define BOOL_STR(b) ((b) ? "true" : "false")
#define CE_SIZE(x) (((x) / 2) + 1)
